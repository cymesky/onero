version: '3'

services:
  db:
    image: postgres
    restart: always
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - ${POSTGRES_DATA}:/var/lib/postgresql/data

  scrapy_service:
    build: scrapy_service/.
    env_file: .env
    depends_on:
      - db

